---
- name: Set kubectl context
  command: kubectl config use-context minikube
  changed_when: false
  
# -----------------------------
# ConfigMaps
# -----------------------------
- name: Apply ConfigMap
  command: kubectl apply -f k8s/configmap.yml
  args:
    chdir: "{{ project_dir }}"

# -----------------------------
# Secrets
# -----------------------------
- name: Apply Secret
  command: kubectl apply -f k8s/secret.yml
  args:
    chdir: "{{ project_dir }}"

- name: Deploy Nginx Config
  command: kubectl apply -f k8s/nginx-configmap.yml
  args:
    chdir: "{{ project_dir }}"

- name: Deploy Nginx
  command: kubectl apply -f k8s/nginx-deployment.yml
  args:
    chdir: "{{ project_dir }}"

- name: Deploy Nginx Service
  command: kubectl apply -f k8s/nginx-service.yml
  args:
    chdir: "{{ project_dir }}"
    
# -----------------------------
# Persistent Volumes
# -----------------------------
- name: Apply PostgreSQL PVC
  command: kubectl apply -f k8s/postgres-pvc.yml
  args:
    chdir: "{{ project_dir }}"

# -----------------------------
# Database
# -----------------------------
- name: Deploy PostgreSQL
  command: kubectl apply -f k8s/postgres-deployment.yml
  args:
    chdir: "{{ project_dir }}"

- name: Deploy PostgreSQL Service
  command: kubectl apply -f k8s/postgres-service.yml
  args:
    chdir: "{{ project_dir }}"

# -----------------------------
# Redis
# -----------------------------
- name: Deploy Redis
  command: kubectl apply -f k8s/redis-deployment.yml
  args:
    chdir: "{{ project_dir }}"

- name: Deploy Redis Service
  command: kubectl apply -f k8s/redis-service.yml
  args:
    chdir: "{{ project_dir }}"

# -----------------------------
# Backend
# -----------------------------
- name: Deploy Backend
  command: kubectl apply -f k8s/backend-deployment.yml
  args:
    chdir: "{{ project_dir }}"

- name: Deploy Backend Service
  command: kubectl apply -f k8s/backend-service.yml
  args:
    chdir: "{{ project_dir }}"

# -----------------------------
# Frontend
# -----------------------------
- name: Deploy Frontend
  command: kubectl apply -f k8s/frontend-deployment.yml
  args:
    chdir: "{{ project_dir }}"

- name: Deploy Frontend Service
  command: kubectl apply -f k8s/frontend-service.yml
  args:
    chdir: "{{ project_dir }}"

# -----------------------------
# Prometheus
# -----------------------------
- name: Deploy Prometheus Config
  command: kubectl apply -f k8s/prometheus-configmap.yml
  args:
    chdir: "{{ project_dir }}"

- name: Deploy Prometheus
  command: kubectl apply -f k8s/prometheus-deployment.yml
  args:
    chdir: "{{ project_dir }}"

- name: Deploy Prometheus Service
  command: kubectl apply -f k8s/prometheus-service.yml
  args:
    chdir: "{{ project_dir }}"

# -----------------------------
# Grafana
# -----------------------------
- name: Deploy Grafana
  command: kubectl apply -f k8s/grafana-deployment.yml
  args:
    chdir: "{{ project_dir }}"

- name: Deploy Grafana Service
  command: kubectl apply -f k8s/grafana-service.yml
  args:
    chdir: "{{ project_dir }}"

# -----------------------------
# Wait for Rollout
# -----------------------------
- name: Wait for Backend
  command: kubectl rollout status deployment/backend
  register: backend_rollout
  changed_when: false

- debug:
    var: backend_rollout.stdout_lines

- name: Wait for Frontend
  command: kubectl rollout status deployment/frontend
  register: frontend_rollout
  changed_when: false

- debug:
    var: frontend_rollout.stdout_lines

- name: Wait for PostgreSQL
  command: kubectl rollout status deployment/postgres
  register: postgres_rollout
  changed_when: false

- debug:
    var: postgres_rollout.stdout_lines

- name: Wait for Redis
  command: kubectl rollout status deployment/redis
  register: redis_rollout
  changed_when: false

- debug:
    var: redis_rollout.stdout_lines

- name: Wait for Prometheus
  command: kubectl rollout status deployment/prometheus
  register: prometheus_rollout
  changed_when: false

- debug:
    var: prometheus_rollout.stdout_lines

- name: Wait for Grafana
  command: kubectl rollout status deployment/grafana
  register: grafana_rollout
  changed_when: false

- debug:
    var: grafana_rollout.stdout_lines

# -----------------------------
# Show Cluster Status
# -----------------------------
- name: Show Pods
  command: kubectl get pods -o wide
  register: pods
  changed_when: false

- debug:
    var: pods.stdout_lines

- name: Show Services
  command: kubectl get svc
  register: services
  changed_when: false

- debug:
    var: services.stdout_lines