---
- name: Install required packages
  tags:
    - setup
  apt:
    name:
      - curl
      - git
      - docker.io
    state: present
    update_cache: yes

- name: Enable Docker
  tags:
    - setup
  service:
    name: docker
    state: started
    enabled: yes
  
- name: Add azureuser to docker group
  tags:
    - setup
  user:
    name: azureuser
    groups: docker
    append: yes

- name: Reset SSH connection
  tags:
    - setup
  meta: reset_connection

# -----------------------------
# Install kubectl
# -----------------------------
- name: Install kubectl
  tags:
    - setup
  shell: |
    curl -LO "https://dl.k8s.io/release/$(curl -Ls https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
    install -o root -g root -m 0755 kubectl /usr/local/bin/kubectl
    rm kubectl
  args:
    creates: /usr/local/bin/kubectl

# -----------------------------
# Install k3s
# -----------------------------
- name: Install k3s
  tags:
    - setup
  shell: |
    curl -sfL https://get.k3s.io | sh -
  args:
    creates: /usr/local/bin/k3s

# -----------------------------
# Configure kubectl
# -----------------------------
- name: Create kube directory
  tags:
    - setup
  file:
    path: /home/azureuser/.kube
    state: directory
    owner: azureuser
    group: azureuser
    mode: '0755'

- name: Copy k3s kubeconfig
  tags:
    - setup
  copy:
    src: /etc/rancher/k3s/k3s.yaml
    dest: /home/azureuser/.kube/config
    remote_src: yes
    owner: azureuser
    group: azureuser
    mode: '0600'

- name: Replace localhost in kubeconfig
  tags:
    - setup
  replace:
    path: /home/azureuser/.kube/config
    regexp: '127\.0\.0\.1'
    replace: '127.0.0.1'

- name: Configure KUBECONFIG
  tags:
    - setup
  lineinfile:
    path: /home/azureuser/.bashrc
    line: 'export KUBECONFIG=$HOME/.kube/config'
    create: yes

# -----------------------------
# Clone Repository
# -----------------------------
- name: Clone RetailIQ repository
  tags:
    - setup
  become: true
  become_user: azureuser
  become_flags: "-i"
  git:
    repo: "{{ repo_url }}"
    dest: "{{ project_dir }}"
    version: "{{ repo_branch }}"
    force: yes
    update: yes

# -----------------------------
# Build Backend Image
# -----------------------------
- name: Build Backend Image
  tags:
    - build
  become: true
  become_user: azureuser
  command: docker build --pull -t retailiq-backend ./backend
  args:
    chdir: "{{ project_dir }}"

# -----------------------------
# Build Frontend Image
# -----------------------------
- name: Build Frontend Image
  tags:
    - build
  become: true
  become_user: azureuser
  command: >
    docker build
    --build-arg NEXT_PUBLIC_API_URL=/api
    -t retailiq-frontend .
  args:
    chdir: "{{ project_dir }}"

# -----------------------------
# Load Images into K3s
# -----------------------------
- name: Export backend image
  tags:
    - build
  become: true
  become_user: azureuser
  become_flags: "-i"
  command: docker save retailiq-backend -o /tmp/backend.tar

- name: Load Backend Image
  tags:
    - build
  become: true
  become_user: azureuser
  become_flags: "-i"
  command: k3s ctr images import /tmp/backend.tar

- name: Export frontend image
  tags:
    - build
  become: true
  become_user: azureuser
  become_flags: "-i"
  command: docker save retailiq-frontend -o /tmp/frontend.tar

- name: Load Frontend Image
  tags:
    - build
  become: true
  become_user: azureuser
  become_flags: "-i"
  command: k3s ctr images import /tmp/frontend.tar

# -----------------------------
# Show Docker Images
# -----------------------------
- name: Show Docker Images
  tags:
    - build
  become: true
  become_user: azureuser
  become_flags: "-i"
  command: docker images
  register: docker_images
  changed_when: false

- debug:
    var: docker_images.stdout_lines